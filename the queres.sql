/* SELECT 
FROM Hr
WHERE Age < 30
GROUP BY FP , Gender , TermDate , PayType
ORDER BY EmpID DESC;*/


/* SELECT 
FROM Hr
WHERE Gender = F 
GROUP BY FP , Gender , TermDate , PayType , AgeGroup
ORDER BY Age ;*/

/* SELECT COUNT EmpID , BURegion
FROM Hr
GROUP BY EthnicGroup
ORDER BY COUNT EmpID DESC; */







